{% extends "base.html.twig" %}

{% block body %}
<h1>New event</h1>

	{{ form_start(form) }}
		{{ form_row(form.name) }}
		{{ form_row(form.date) }}
		{{ form_row(form.latitude) }}
		{{ form_row(form.longitude) }}

		<button type="submit">
			Submit
		</button>
	{{ form_end(form) }}
{% endblock %}